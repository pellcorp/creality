Diff against upstream/main (pellcorp/creality), scoped to the files
touched to add the hx711_driver probe option (config, macros, F001 mount
override, and the installer.sh wiring that recognizes --probe
hx711_driver). Generated via:
  git diff upstream/main -- config/hx711_driver.cfg \
    config/hx711_driver_macro.cfg mounts/hx711_driver/ k1/installer.sh

diff --git a/config/hx711_driver.cfg b/config/hx711_driver.cfg
new file mode 100644
index 0000000..d6847be
--- /dev/null
+++ b/config/hx711_driver.cfg
@@ -0,0 +1,71 @@
+[load_cell_probe]
+sensor_type: hx711s
+# order: FR, FL, RL, RR -- real F001 leveling_mcu pins
+dout_pins: leveling_mcu:PB12, leveling_mcu:PB14, leveling_mcu:PA8, leveling_mcu:PA10
+sclk_pins: leveling_mcu:PB13, leveling_mcu:PB15, leveling_mcu:PA9, leveling_mcu:PA11
+sensor_orientation: inverted
+sample_rate: 80
+gain: A-128
+
+# PLACEHOLDER -- must be re-measured on real hardware via LOAD_CELL_CALIBRATE
+# once the hx711s firmware is flashed. One shared counts_per_gram now covers
+# all 4 summed channels, not four independent per-corner fits.
+counts_per_gram: 999
+reference_tare_counts: 999, 999, 999, 999
+
+# trigger_force has a hard maxval of 250 (load_cell_probe.py's own
+# intParamHelper) -- this is the module's own built-in default, re-tune with
+# LOAD_CELL_TEST_TAP once real hardware data exists
+trigger_force: 75
+force_safety_limit: 2000
+drift_safety_limit: 1000
+
+# OpenCentauri's real values (machine.cfg) -- Load_Cell.md explicitly
+# recommends capping HX711 probing speed at 2mm/s
+speed: 2.0
+lift_speed: 10.0
+z_offset: 0.0
+pullback_distance: 0.4
+pullback_speed: 1.0
+sample_retract_dist: 0.5
+samples_tolerance: 0.05
+samples_tolerance_retries: 3
+# module default -- OpenCentauri's own docs/configs never override this.
+# tap_quality stays inert (no scoring) until decompression_angle is
+# calibrated, so this is the real, documented safe-starting-point path.
+tap_classifier: tap_quality
+
+[bed_mesh]
+# OpenCentauri's real values (machine.cfg) -- fade left at module default
+# (disabled) since OpenCentauri never overrides it either
+speed: 150
+mesh_min: 999,999
+mesh_max: 999,999
+horizontal_move_z: 2
+probe_count: 9,9
+mesh_pps: 3,3
+algorithm: bicubic
+adaptive_margin: 3
+move_check_distance: 3.0
+split_delta_z: 0.01
+horizontal_z_clearance: 0.4
+
+[screws_tilt_adjust]
+screw1: 999,999
+screw1_name: front left screw
+screw2: 999,999
+screw2_name: front right screw
+screw3: 999,999
+screw3_name: rear right screw
+screw4: 999,999
+screw4_name: rear left screw
+speed: 50
+horizontal_move_z: 5
+screw_thread: CW-M4
+
+[axis_twist_compensation]
+speed: 50
+horizontal_move_z: 5
+calibrate_start_x: 999
+calibrate_end_x: 999
+calibrate_y: 999
diff --git a/config/hx711_driver_macro.cfg b/config/hx711_driver_macro.cfg
new file mode 100644
index 0000000..f37bf17
--- /dev/null
+++ b/config/hx711_driver_macro.cfg
@@ -0,0 +1,24 @@
+[gcode_macro BED_MESH_CALIBRATE]
+rename_existing: _BED_MESH_CALIBRATE
+gcode:
+    {% if printer["gcode_macro _SAF_BED_MESH_START"] != null %}
+      _SAF_BED_MESH_START
+    {% endif %}
+
+    _BED_MESH_CALIBRATE {rawparams}
+
+    {% if printer["gcode_macro _SAF_BED_MESH_END"] != null %}
+      _SAF_BED_MESH_END
+    {% endif %}
+
+# [load_cell_probe] has no name suffix, so LOAD_CELL_TARE/READ/DIAGNOSTIC/
+# CALIBRATE are all real, bare stock commands -- no LOAD_CELL=<name> needed,
+# and no wrapper macro needed either. Kept below only as documentation.
+#
+# LOAD_CELL_TARE           -- zero the combined signal
+# LOAD_CELL_CALIBRATE      -- guided weight calibration (TARE / CALIBRATE
+#                              GRAMS=<n> / ACCEPT), or TARE=TRUE SAVE=TRUE
+#                              for a quick retare-and-persist
+# LOAD_CELL_READ           -- one-shot reading, per-channel breakdown included
+# LOAD_CELL_DIAGNOSTIC     -- sensor health, error/overflow counts
+# LOAD_CELL_TEST_TAP       -- fixed-position tap, boolean trigger smoke test
diff --git a/k1/installer.sh b/k1/installer.sh
index fda52f9..63c8b66 100755
--- a/k1/installer.sh
+++ b/k1/installer.sh
@@ -828,16 +828,33 @@ function install_klipper() {
 
         if [ ! -d /usr/data/klipper/.git ]; then
             echo "INFO: Installing ${klipper_fork} ..."
-            git clone https://github.com/pellcorp/${klipper_fork}.git /usr/data/klipper || exit $?
+            klipper_owner=pellcorp
+            [ "$klipper_fork" = "kalico" ] && klipper_owner=sterling5241
+            git clone https://github.com/${klipper_owner}/${klipper_fork}.git /usr/data/klipper || exit $?
 
             [ -d /usr/share/klipper ] && rm -rf /usr/share/klipper
         else
             cd /usr/data/klipper/
             branch_ref=$(git rev-parse --abbrev-ref HEAD)
             remote_repo=$(git remote get-url origin | awk -F '/' '{print $NF}' | sed 's/.git//g')
+            remote_owner=$(git remote get-url origin | awk -F '/' '{print $(NF-1)}')
             klipper_status=0
             cd - > /dev/null
 
+            # a bootstrap-server first-boot checkout always clones the
+            # upstream owner, not any --kalico fork -- fix that up here,
+            # since this is the only path a pre-existing checkout takes
+            klipper_owner=pellcorp
+            [ "$klipper_fork" = "kalico" ] && klipper_owner=sterling5241
+            if [ "$remote_repo" = "$klipper_fork" ] && [ "$remote_owner" != "$klipper_owner" ]; then
+                echo "INFO: Repointing klipper from ${remote_owner}/${remote_repo} to ${klipper_owner}/${remote_repo}"
+                cd /usr/data/klipper/
+                git remote set-url origin https://github.com/${klipper_owner}/${remote_repo}.git || exit $?
+                git fetch origin || exit $?
+                git reset --hard origin/${branch_ref} || exit $?
+                cd - > /dev/null
+            fi
+
             # force update
             if [ ! -f /usr/data/klipper/fw/K1/klipper_host_mcu ]; then
               klipper_status=1
@@ -1304,6 +1321,7 @@ function cleanup_probes() {
   cleanup_probe beacon
   cleanup_probe klicky
   cleanup_probe bltouch
+  cleanup_probe hx711_driver
 }
 
 function setup_bltouch() {
@@ -1409,6 +1427,27 @@ function setup_klicky() {
     return 0
 }
 
+function setup_hx711_driver() {
+    grep -q "hx711_driver-probe" /usr/data/pellcorp.done
+    if [ $? -ne 0 ]; then
+        echo
+        echo "INFO: Setting up hx711_driver ..."
+
+        cp /usr/data/pellcorp/config/hx711_driver.cfg /usr/data/printer_data/config/ || exit $?
+        $CONFIG_HELPER --add-include "hx711_driver.cfg" || exit $?
+
+        cp /usr/data/pellcorp/config/hx711_driver_macro.cfg /usr/data/printer_data/config/ || exit $?
+        $CONFIG_HELPER --add-include "hx711_driver_macro.cfg" || exit $?
+
+        echo "hx711_driver-probe" >> /usr/data/pellcorp.done
+        sync
+
+        # means klipper needs to be restarted
+        return 1
+    fi
+    return 0
+}
+
 function set_serial_cartotouch() {
     local SERIAL_ID=$(ls /dev/serial/by-id/usb-* | grep "IDM\|Cartographer" | head -1)
     if [ -n "$SERIAL_ID" ]; then
@@ -2118,7 +2157,7 @@ fi
         elif [ "$1" = "--force" ]; then
           force=true
           shift
-        elif [ "$1" = "microprobe" ] || [ "$1" = "bltouch" ] || [ "$1" = "beacon" ] || [ "$1" = "klicky" ] || [ "$1" = "cartographer" ] || [ "$1" = "cartotouch" ] || [ "$1" = "btteddy" ] || [ "$1" = "eddyng" ]; then
+        elif [ "$1" = "microprobe" ] || [ "$1" = "bltouch" ] || [ "$1" = "beacon" ] || [ "$1" = "klicky" ] || [ "$1" = "cartographer" ] || [ "$1" = "cartotouch" ] || [ "$1" = "btteddy" ] || [ "$1" = "eddyng" ] || [ "$1" = "hx711_driver" ]; then
             if [ "$mode" = "fix-serial" ]; then
                 echo "ERROR: Switching probes is not supported while trying to fix serial!"
                 exit 1
@@ -2524,6 +2563,28 @@ fi
     elif [ "$probe" = "klicky" ]; then
         setup_klicky
         setup_probe_specific=$?
+    elif [ "$probe" = "hx711_driver" ]; then
+        if [ "$klipper_fork" != "kalico" ]; then
+            echo "ERROR: hx711_driver requires --kalico, stock Klipper does not have the load_cell support this needs"
+            exit 1
+        fi
+        if [ "$MODEL" != "F001" ]; then
+            echo "ERROR: hx711_driver is only supported on F001 for now"
+            exit 1
+        fi
+        echo "***************************************************************"
+        echo "* WARNING: hx711_driver is EXTREMELY EXPERIMENTAL.             *"
+        echo "* The author takes no responsibility if your machine is     *"
+        echo "* damaged as a result of using it.                          *"
+        echo "***************************************************************"
+        printf "Continue? [y/N] "
+        read confirm
+        if [ "$confirm" != "y" ] && [ "$confirm" != "Y" ]; then
+            echo "Aborted."
+            exit 1
+        fi
+        setup_hx711_driver
+        setup_probe_specific=$?
     else
         echo "ERROR: Probe $probe not supported"
         exit 1
diff --git a/mounts/hx711_driver/Default-f001.overrides b/mounts/hx711_driver/Default-f001.overrides
new file mode 100644
index 0000000..464ab66
--- /dev/null
+++ b/mounts/hx711_driver/Default-f001.overrides
@@ -0,0 +1,11 @@
+-- hx711_driver.cfg
+[bed_mesh]
+mesh_min: 5,5
+mesh_max: 215,215
+zero_reference_position: 110,110
+
+[screws_tilt_adjust]
+__action__: DELETED
+
+[axis_twist_compensation]
+__action__: DELETED
